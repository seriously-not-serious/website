---
type Props = {
    className?: string;
};

const { className = "" } = Astro.props as Props;
---

<div
    class={`p-4 bg-gray-800 rounded-lg shadow-md flex flex-col gap-2 ring-1 ring-gray-900 hover:ring-gray-700 duration-300 hover:shadow-black ${className}`}
>
    <slot />
</div>
